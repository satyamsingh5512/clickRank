global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'clickrank-backend'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8081'] # Assumes running app locally on port 8081