# Frontend environment — Vite reads VITE_* vars at build time
# Rename to .env.local for local development (gitignored by default in Vite)

# URL the browser sends API requests to (the API Gateway)
VITE_API_BASE_URL=http://localhost:8080
